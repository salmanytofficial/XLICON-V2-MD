
import fetch from 'node-fetch'; let handler = async (m) => { let message = m.quoted ? m.quoted : m; let text = (message.text || '').trim(); if (!text) { throw "✳️ Please provide text for the AI to process."; } await m.react('⏳'); try { let response = await fetch(`https://bk9.fun/ai/Text2Img?q=${encodeURIComponent(text)}`); let BK8 = await response.json(); await m.react('✅'); const BK7 = { text: BK8.BK9 || "No response received from the API." }; await conn.sendMessage(m.chat, BK7, { quoted: m }); } catch (error) { await m.react('❌'); throw `❌ error ; } }; handler.help = ['img']; handler.tags = ['ai']; handler.command = /^(img)$/i; export default handler;
